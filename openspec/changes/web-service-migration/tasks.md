## 0. 仕様整理（今回）

- [x] 現行コード・既存仕様を確認する。
- [x] 指定事項・設計提案・判断事項を区別する。
- [x] 構成・データ・API・環境・移行・受け入れ条件を整理する。
- [x] `openspec validate web-service-migration --strict`を通過する（Node 24.20.0 / OpenSpec 1.12.0）。

## 1. データと実行基盤

- [x] D1マイグレーション、非同期DB層、bar_id必須の在庫・カタログ取得を実装する。
- [x] users/barsの一意性、客人/注文の複合FK、インデックスを実装する。
- [x] HonoをWorkerへ移し、HTTP処理からnode:sqlite・ローカルfs・常駐Node依存を除く。
- [x] Pages中継、API Worker、環境別Wrangler設定・binding・ローカル起動を作る。
- [x] CSV検証、カタログversion配備・切替を管理CLIへ分離する。

## 2. 家主と客人

- [x] Firebase Googleログイン、IDトークン検証・更新、冪等bootstrapを実装する。
- [x] 全家主APIの所有権検証、停止済みユーザーの拒否、ログアウト時の状態消去を実装する。
- [x] バー名、受付ON/OFF、招待再発行、バー別Cookieと失効を実装する。
- [x] PUBLIC_APP_URLの検証と自動inviteUrl、QRの完全パス保持を実装する。
- [x] 客人ルートと既存の全クエリーをバー単位へ移し、旧単一バーAPIを廃止する。
- [x] 在庫versionによる注文確定、冪等再送、代用品・完了の競合制御を実装する。
- [x] Origin検証、分散レート制限、機密を除くログ、キャッシュ制御を実装する。
- [x] 非表示時のポーリング停止と失敗時のバックオフを実装する。

## 3. 買い足しと画像

- [x] 厳密探索をWeb Workerへ移し、version照合・20秒制限・キャンセル・多重起動防止を実装する。
- [x] R2を構成から外し、外部画像URLと既存の代替表示を維持する。

## 4. 移行と運用

- [ ] 移行範囲・ドメイン・予算・画像用途を確定する。
- [x] SQLiteのバックアップ、指定バーへのdry-run付き移行、再実行検証を用意する。
- [x] DB復元・アプリ切り戻し・受付停止・障害対応の手順を文書化する。
- [ ] stagingで復元・切り戻しを実施する（環境設定待ち）。
- [ ] 保存期間・削除・退会対応を決め、公開案内を整備する。
- [x] READMEをクラウド運用中心へ更新しLAN手順を旧運用として整理する。
- [x] 実装済み要件をopenspec/specsへ統合し、旧要件の矛盾を解消する。

## 5. 検証と公開

- [x] 家主A/Bの在庫・注文・招待・買い足し入力の相互アクセス拒否を統合テストする。
- [x] 同一ブラウザーのバーA/B参加、同名客人、再発行、期限切れ、受付停止を検証する。
- [x] 在庫と注文の競合、同時再送、重複bootstrap、代用品・完了を検証する。
- [x] 既存の在庫判定・スナップショット・購入最適化を回帰検証する。
- [x] 本番相当Workers環境のD1/Service binding、SPA直リンク、Cookieを確認する。
- [x] ブランド付きQRを画像からデコードし、スマホ幅のChromiumで参加・注文・提供完了・招待失効を確認する。
- [x] テスト用Firebase認証状態で家主UIの在庫・設定・ログアウト・アカウント切替を確認する。
- [ ] 本番URLとスマホ実機Safari/ChromeのGoogleログインを確認する（実アカウント・環境設定待ち）。
- [x] 分散レート制限の具体値を公開手順へ記録する。
- [ ] 想定利用規模に基づくstaging負荷・費用を確認する。
- [x] 型チェック・ビルド・テスト・OpenSpec厳密検証をローカルで通し、CIワークフローを追加する。
- [ ] リモートCIの実行結果を確認する（push未実施）。
- [ ] stagingに配備し受け入れ条件と復旧を確認する。
- [ ] 本番配備・必要な最終データ移行・疎通確認を行い結果を記録する。
