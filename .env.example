# Read by the Node.js server, data importer and backup command.
HOST=0.0.0.0
PORT=3001
DB_PATH=.runtime/bar.sqlite
# Set this to the Windows PC's current LAN address and the exposed port.
# PUBLIC_URL=http://<PC_LAN_ADDRESS>:3001
