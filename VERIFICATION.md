# Webサービス版の検証（2026-09-08）

## ローカルで確認済み

- Node.js 24.20.0、全35件の自動テストが成功。従来機能29件に、D1のマルチテナント分離・認証・招待・注文競合・カタログ移行・旧SQLite移行の検証を追加。
- TypeScript型チェック、Vite本番ビルド、Prettier、OpenSpec全18項目の厳密検証が成功。
- ローカルD1にマイグレーションと実CSV（209材料・955カクテル）を配備し、再実行できることを確認。
- 家主A/Bで在庫・メニュー・注文・代用品の分離、並行bootstrap、同名客人、Cookieのバー別パス・有効期限、招待再発行、受付停止、ユーザー停止、Origin検証を確認。
- 並行再送で注文が1件になること、在庫versionを確定直前に変更すると409になること、D1共有カウンターで429・Retry-Afterを返すことを確認。
- JWTの署名・期限・発行元・対象プロジェクト・Googleプロバイダーの検証を、テスト用RSA鍵で確認。
- ビルド済みPages Functions→Service binding→ビルド済みAPI Worker→D1をworkerd/Miniflareで稼働させ、署名付きIDトークンの認証、bootstrap、招待、Set-CookieとCookieの往復を確認。公開鍵取得先の応答はテスト用に模擬。
- 390px幅のChromiumで参加→注文→提供完了の自動更新、同じブラウザーで2つのバーのCookie維持、招待失効後の再参加案内、直リンクを確認。
- ブランド付きQRのSVGを画像化し、異なる長さの招待URLをjsQRでデコード。家主画面上のQRも完全な招待URLと一致。固定ナビが重ならない位置までスクロールして読み取り検証。
- コンパイル済みの買い足しWeb Workerをブラウザーで実行。厳密探索は独立した全列挙との照合を含む既存テストでも検証。
- 家主UIはテスト専用ビルドでFirebase SDKの保存ユーザーと認証応答を模擬し、在庫・バー名変更・受付停止・QR・ログアウト・別家主への切替を確認。本番ビルドにはテスト認証設定を含めない。
- 日本語フォントをPages同梱へ変更し、スマホ画面を目視確認。招待画面から外部フォントへの通信は不要。
- 旧SQLiteのdry-run、整合したバックアップ、誤った移行先UIDの拒否、部分移行後の再実行、注文スナップショット保持、旧Cookieが移行されないことを確認。
- CIワークフローを追加。リモートCIはpushしていないため未実行。

## 実環境で残る確認

- Cloudflareの実リソース・公開ドメイン・Firebase Webアプリの設定。
- 実際のGoogleアカウントでのpopupログイン、スマホ実機Safari/Chromeでの一連の操作。
- staging / productionへの配備、実データの最終移行、復元・切り戻しリハーサル。
- 想定利用者数に応じた負荷・料金、保存期間・削除窓口の確定。

R2は使用しない。実アカウント・本番在庫・本番注文を変更していない。手順は[DEPLOYMENT.md](docs/DEPLOYMENT.md)を参照。

---

# 旧LAN版の検証記録（履歴）

## 完了

- 現在のCSVマスターとマニフェストのSHA-256一致（画像URL更新前のハッシュも保持）。955カクテル・209材料・2,922レシピ行を取り込み、外部キー・重複ID・レシピ欠落の検証を通過。
- 21件の自動テストが成功（`./scripts/npm.sh run test`）。完全一致／同種代用／未分類／常備品、検索・度数範囲、同名客人の分離、注文時の在庫再判定、通信再送時の重複防止、代用品選択、提供完了の再送、ファイルDB再接続後の保持、マスター変更後の注文スナップショット保持、不正入力を確認。
- TypeScript型チェックとVite本番ビルド成功。
- OpenSpec `validate add-home-cocktail-ordering --strict` 成功。
- SQLiteバックアップAPIでバックアップファイルを作成できることを確認。
- Windows PowerShellのパーサーでEnable-Lan.ps1の構文検証成功。
- WSL側の `http://localhost:3001/api/health` と `/host` はHTTP 200。Windows側のlocalhostからもhealthのHTTP 200を確認。

## 未実施

- Windows管理者権限でのLAN転送・ファイアウォール設定。利用環境で管理者による設定が必要。
- 同じWi-Fiのスマートフォン実機でQR参加・在庫操作・注文・提供完了・表示更新を通しで確認すること。
- ブラウザーでの見た目・タップ・フォーカス操作、複数ブラウザーのUI連携。モバイル用CSSと操作UIは実装済みだが、ブラウザー操作による検証は行っていない。
- 任意のWebMCP検索連携は未対応ブラウザーで無効になるよう実装。対応した検証コンテキストがないため登録・実行は未検証。

## 運用データ

自動テストはメモリーDBまたはテスト専用一時DBを使用。通常利用DBへサンプルの在庫・客人・注文は作成していない。

## 画像URL更新の実行結果

- 全955件を対象に新配信先の画像GETを実施。954件でHTTP成功・画像Content-Type・バイナリ署名を確認し、CSVの画像URLを更新。
- 1件（ノンアル・ライチジャスミン）は通常名・番号付き候補が404のため元URLを維持。
- 画像URL以外の列が更新前と一致することを確認。
- DBバックアップ後に再取り込みを実行。稼働中APIが新しい画像URLを返すことを確認。
- 詳細: `data/image-url-update-report.json`。

## 買い足し提案（2026-09-07）

- 購入上限1〜10・初期値5。全29件の自動テストに、探索の相乗効果、同点時の少ない購入数、200個の問題と全列挙の照合、時間超過、代用・常備品・null分類・空レシピ、在庫変更、APIの重複実行と応答性の検証を含む。
- テスト専用のメモリーDBで、提案どおりの購入後に既存の作成可能判定が返すカクテルID・件数と照合。
- Node.js 24.20.0・現在のCSV（955カクテル、209材料、128代用グループ）で、実装版の厳密探索を計測。常備品だけの空在庫と、レシピへの登場頻度上位の材料グループを所持した在庫を使用。

| 所持グループ数（常備品を除く） | 5種類購入 | 10種類購入 |
| --- | ---: | ---: |
| 0 | 209 ms | 5,021 ms |
| 5 | 87 ms | 2,854 ms |
| 15 | 26 ms | 230 ms |
| 30 | 9 ms | 81 ms |

- 最終確認で全29テスト、TypeScript型チェック、Viteビルド、Prettier、OpenSpec `validate add-purchase-recommendations --strict` が成功。
- 生の計測結果は `openspec/changes/add-purchase-recommendations/benchmark.json`。機材・在庫・データ変更によって計算時間は変わる。
- テスト専用DBと127.0.0.1:3019の一時サーバーで、ヘッドレスChromeの390px・1280px幅を確認。初期値5、5/10種類の計算、カクテル・材料の展開、別操作による在庫変更後の結果無効化と再計算、横はみ出しなし、JavaScript例外なしを確認。390pxのスクリーンショットも目視確認。
- 今回の購入提案はブラウザー確認済み。上記「未実施」のアプリ全体の操作・スマートフォン実機検証は引き続き未実施。
- 本番の在庫・注文データは変更していない。
