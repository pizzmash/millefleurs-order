# 検証記録

## 完了

- コピー元CSVとのSHA-256一致。955カクテル・209材料・2,922レシピ行を取り込み、外部キー・重複ID・レシピ欠落の検証を通過。
- 15件の自動テストが成功（`./scripts/npm.sh run test`）。完全一致／同種代用／未分類／常備品、検索・度数範囲、同名客人の分離、注文時の在庫再判定、通信再送時の重複防止、代用品選択、提供完了の再送、ファイルDB再接続後の保持、マスター変更後の注文スナップショット保持、不正入力を確認。
- TypeScript型チェックとVite本番ビルド成功。
- OpenSpec `validate add-home-cocktail-ordering --strict` 成功。
- SQLiteバックアップAPIでバックアップファイルを作成できることを確認。
- Windows PowerShellのパーサーでEnable-Lan.ps1の構文検証成功。
- WSL側の `http://localhost:3001/api/health` と `/host` はHTTP 200。Windows側のlocalhostからもhealthのHTTP 200を確認。

## 未実施

- Windows管理者権限でのLAN転送・ファイアウォール設定。現在のWindows実行権限は通常ユーザー。既存3000番転送設定は変更していない。
- 同じWi-Fiのスマートフォン実機でQR参加・在庫操作・注文・提供完了・表示更新を通しで確認すること。
- ブラウザーでの見た目・タップ・フォーカス操作、複数ブラウザーのUI連携。モバイル用CSSと操作UIは実装済みだが、ブラウザー操作による検証は行っていない。
- 任意のWebMCP検索連携は未対応ブラウザーで無効になるよう実装。対応した検証コンテキストがないため登録・実行は未検証。

## 運用データ

自動テストはメモリーDBまたはテスト専用一時DBを使用。通常利用DBへサンプルの在庫・客人・注文は作成していない。
